# GIT_WORKFLOW — AfricaFunds

Workflow normal : lire/réconcilier → vérifier les deux HEAD → single writer → modification compatible → tests/non-régression → persistance → commit sur branche canonique → relecture remote → déploiement séparément autorisé → attestation. Pas de nouvelle branche, force-push ou rewrite.