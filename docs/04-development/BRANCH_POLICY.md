# BRANCH_POLICY — AfricaFunds

Branches canoniques : `claude/code-review-improvements-ikvuj` dans API et frontend. `NEW_BRANCH_CREATION=FORBIDDEN`, `FORCE_PUSH=FORBIDDEN`, `HISTORY_REWRITE=FORBIDDEN`. Le préfixe historique `claude/` ne confère aucune propriété à un agent. Tout changement de branche canonique exige décision explicite et migration de gouvernance.