# BUILD — AfricaFunds API

Le build réel est défini par `package.json`, scripts et workflows existants. Avant release, utiliser les commandes versionnées ; ne jamais inventer une commande de build documentaire. Conserver logs/résultats significatifs comme preuve.