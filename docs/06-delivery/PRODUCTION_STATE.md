# PRODUCTION_STATE — AfricaFunds API

L’état production est une **mesure**, pas une hypothèse. Autorités : runtime S2 récent + `docs/ETAT_PRODUCTION_VERIFIE.md` lorsqu’il reflète la mesure + GOV-006. Le snapshot technique historique `/var/lib/fundafrica/runtime/PRODUCTION_STATE.json` conserve son chemin réel. `HTTP 200 != égalité GitHub/S2`.