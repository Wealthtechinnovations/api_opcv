# CI_CD — AfricaFunds API

Les workflows `.github/workflows/` existants sont les mécanismes réels. Cette documentation les indexe et impose : pas de duplication de pipeline, checks déterministes, secrets via GitHub/infra autorisée, déploiement séparé du simple commit et attestation après production.