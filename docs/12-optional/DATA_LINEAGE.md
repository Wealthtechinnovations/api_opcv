# DATA_LINEAGE — AfricaFunds

`APPLICABLE`. Pour toute donnée critique : source → ingestion → transformation → stockage → calcul → API → affichage/export. Conserver versions, dates, devises/unités et preuves. Les transformations silencieuses non traçables sont interdites.